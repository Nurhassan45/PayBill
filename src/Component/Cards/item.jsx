import React from 'react';

const Item = ({res}) => {
    return (
        <div className='badge badge-outline badge-primary'>
            <h1>{res}</h1>
            
        </div>
    );
};

export default Item;